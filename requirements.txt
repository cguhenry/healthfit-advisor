# Core runtime dependencies for the default skill / CLI experience.
# Keep this file minimal; optional feature stacks live in dedicated files
# or pyproject extras so production installs stay lean.
requests>=2.31.0
